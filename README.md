# ARMORA — оклейка и защита кузова

Сайт студии, которая клеит защитную плёнку на автомобили: шесть страниц,
расчёт стоимости за три шага, комплексы с ценами по классам авто, работы,
рассказ о боксе и заявка.

**Реализация по мотивам концепта** [«Landing Page auto | Сайт оклейка авто»](https://www.behance.net/gallery/246422753/Landing-Page-auto-Cajt-oklejka-avto-Car-wrapping),
автор макета — Татьяна Кирхеснер. Вёрстка, калькулятор, форма и тексты мои.
Студия вымышленная, фотографии со стока.

## Структура

| Файл | Что внутри |
|---|---|
| [index.html](index.html) | Герой, калькулятор, комплексы, доверие, процесс, работы, заявка, отзывы |
| [services.html](services.html) | Все услуги, классы автомобилей, материалы, частые вопросы |
| [works.html](works.html) | Восемь работ с комплексом и сроком по каждой |
| [about.html](about.html) | Как устроена работа, команда, чего мы не делаем |
| [contacts.html](contacts.html) | Контакты, форма заявки, как доехать |
| [privacy.html](privacy.html) | Политика обработки персональных данных |
| [css/style.css](css/style.css) | Токены, компоненты, адаптив |
| [js/main.js](js/main.js) | Меню, появление блоков, табы, калькулятор, форма |
| [assets/photos/](assets/photos/) | Фотографии со стока |

## Как открыть

Достаточно открыть `index.html` — зависимостей и сборки нет. Локальный сервер,
если нужен:

```bash
python -m http.server 8381
```

## Дизайн-система

Тёмная основа снята пипеткой с макета (`#0c0c0c`), акцент заменён на золотой
по просьбе заказчика.

| Токен | Значение | Где |
|---|---|---|
| `--bg` | `#0a0a0b` | Фон страницы |
| `--surface` | `#121214` | Карточки |
| `--accent` | `#dbb04f` | Заливка и текст |
| `--accent-text` | `#dbb04f` | То же значение, отдельный токен |
| `--on-accent` | `#14100a` | Текст на золотой заливке |

**Почему токена два, если цвет один.** Пока акцент был малиновым, его
приходилось раздваивать: как текст `#ec0044` давал 4.14:1 и не проходил
норму, для мелкого кегля нужен был осветлённый вариант. Золото такой
проблемы не создаёт — 9.21:1 на карточке и 9.74:1 на фоне. Разделение
оставлено, чтобы смена акцента снова была возможна одной строкой.

Текст на золотой кнопке тёмный: `#14100a` даёт 9.33:1.

**Шрифт** — [Golos Text](https://fonts.google.com/specimen/Golos+Text). В макете
пара Golos Text + PT Root UI; второго нет в Google Fonts, поэтому весь текст
набран Golos Text, у него есть и заголовочные, и текстовые начертания.

## Калькулятор

Три шага: класс автомобиля, комплекс, тип плёнки. Цены совпадают с прайсом в
блоке «Комплексы» — это один источник для посетителя, и расхождение читалось бы
как обман.

Результат — вилка, а не одно число: точная смета зависит от состояния кузова,
и обещать копейку в копейку до осмотра нечестно.

| Комплекс | I класс | II класс | III класс |
|---|---|---|---|
| Зоны риска | 20 000 ₽ | 30 000 ₽ | 40 000 ₽ |
| Стандарт | 80 000 ₽ | 90 000 ₽ | 100 000 ₽ |
| Полная защита | 210 000 ₽ | 230 000 ₽ | 250 000 ₽ |

Плёнка: прозрачный полиуретан ×1, матовый ×1.15, цветной винил ×0.85.

## Про шесть страниц без сборщика

Шапка и подвал повторяются в каждом файле. Это осознанный размен: собирать
их из шаблонов означало бы завести сборщик, а рисовать скриптом — получить
мигание при загрузке и потерю для поисковиков. При правке меню нужно пройти
по всем шести файлам; при таком размере это дешевле, чем инструментарий.

Текущий раздел в меню помечен `aria-current="page"` и подсвечен — иначе на
пятой странице теряешься.

## Заметки по реализации

- **Свечение под машиной** — радиальный градиент, а не картинка: масштабируется
  без веса и подхватывает акцентный цвет из переменной.
- **`img { height: auto }` обязателен.** У картинок проставлены `width` и
  `height` — они резервируют место и не дают странице дёргаться при загрузке.
  Но без `height: auto` браузер считает высоту заданной и молча игнорирует
  `aspect-ratio`: карточки тарифов вытягивались вдвое.
- **`[hidden] { display: none !important }`** — не перестраховка. Браузер
  задаёт `hidden` через свою таблицу стилей, а любое авторское правило
  `display` перебивает её целиком: у `.btn` стоит `inline-flex`, и кнопка
  «Назад» оставалась видна на первом шаге калькулятора.
- **Появление блоков** страхуется таймером на 1,6 с: если наблюдатель не
  сработал, всё показывается принудительно. Контент не имеет права остаться
  невидимым из-за сбоя в скрипте.
- **Форма** проверяет имя, телефон и согласие, маскирует номер и имеет две
  тихие проверки против ботов — скрытое поле и отсечку по времени. Обе отвечают
  «принято» и ничего не отправляют: бот, получивший отказ, подбирает обход.
- Бэкенда пока нет: форма подтверждает приём и очищается, подключение займёт
  один запрос.

## Проверено

- калькулятор считает по прайсу: кроссовер + полная защита + прозрачный
  полиуретан → вилка 218 500 — 257 600 ₽ при базе 230 000 ₽;
- маска телефона: `89261234567` → `+7 (926) 123-45-67`;
- форма ругается на пустое имя и на отсутствие согласия, принимает корректную;
- табы переключают панели, стрелки работают;
- на 378 px горизонтального скролла нет, все 14 картинок грузятся;
- калькулятор проходится целиком: «Назад» скрыт на первом шаге, вилка
  16 150 — 19 040 ₽ для седана с цветным винилом при базе 20 000 ₽, сброс
  возвращает всё в исходное;
- контраст: обойдено 140 текстовых элементов, ниже нормы нет ни одного;
- обход всех шести страниц: 427 текстовых элементов, ниже нормы контраста
  нет ни одного, горизонтального скролла нет, у всех картинок есть подписи,
  на каждой странице ровно один заголовок первого уровня;
- 91 внутренняя ссылка и якорь — все ведут в существующие места;
- в консоли ошибок нет.
